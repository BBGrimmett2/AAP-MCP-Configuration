apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aap-operator

resources:
  - ../../base

patches:
  - path: configmap.yaml
    target:
      kind: ConfigMap
      name: aap-mcp-custom-config
